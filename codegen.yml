schema: 'src/graphql/**/*.graphql'
generates:
  src/__generated__/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: '../types#GraphQLContext'
      scalars:
        Date: string
      mappers:
        Habit: './models/habit#HabitLean'
        HabitLog: './models/habitLog#HabitLogLean'
